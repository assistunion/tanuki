class User_Model_<%= class_name %>_Base < Tanuki_Object
  extend Tanuki::ModelBehavior

  class << self

    def extract_key(data)
        [<% key.each do |field_name| %>data[:<%= field_name.inspect %>], <% end %>]
    end

    def get(ctx,*args)
    end

  end # end class << self

end # end User_Model_<%= class_name %>_Base